// 담당: 지도표시팀
import { Router } from "express";

const router = Router();

// TODO: GET /grids — 격자 목록 조회
// TODO: GET /grids/:id — 격자 상세 조회
// TODO: GET /grids/:id/safety — 격자 안전 정보 조회

export default router;
